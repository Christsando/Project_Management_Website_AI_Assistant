<table class="w-full">
    <thead>
        <tr class="text-sm text-slate-500 tracking-wide border-b-2">
            <th class="px-3 font-light">{{ __('Project Name') }}</th>
            <th class="px-6 font-light">{{ __('Task') }}</th>
            <th class="px-4 font-light">{{ __('Duedate') }}</th>
            <th class="px-4 font-light">{{ __('Status') }}</th>
            <th class="px-4 font-light">{{ __('Priority') }}</th>
        </tr>
    </thead>
</table>